---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import DownloadTable from '../components/DownloadTable.astro';
import InstallModal from '../components/InstallModal.astro';
import Toast from '../components/Toast.astro';
---

<Layout title="Antigravity for Linux — AppImage & Linux Packages (Unofficial)">
  <div class="min-h-screen bg-[#08080a] flex flex-col">
    <!-- Main Sticky Header Navigation -->
    <Header />

    <!-- Main Content Body (Focused purely on downloads) -->
    <main class="flex-1 max-w-5xl w-full mx-auto px-6 py-10 space-y-10">
      
      <!-- Hero Banner Section -->
      <section class="text-center py-6 space-y-4 border-b border-zinc-900 pb-10">
        <div class="inline-flex items-center space-x-2 bg-amber-950/40 border border-amber-800/40 px-3 py-1 rounded-full text-xs text-amber-300">
          <img src="/images/Antigravity_CLI.svg" alt="Antigravity CLI Logo" class="w-4 h-4 object-contain" />
          <span>Proyecto No Oficial • Descargas AppImage para Linux</span>
        </div>
        
        <h1 class="text-3xl sm:text-4xl font-extrabold text-white tracking-tight">
          Antigravity for Linux
        </h1>

        <p class="text-sm text-zinc-400 max-w-2xl mx-auto leading-relaxed">
          Publicación comunitaria no oficial de paquetes <span class="text-zinc-200 font-semibold">AppImage</span> y binarios para distribuciones Linux, empaquetados directamente a partir de las versiones <code class="text-blue-300 bg-blue-950/60 px-1 py-0.5 rounded">.tar.gz</code> oficiales distribuidas por Google.
        </p>
      </section>

      <!-- Section 1: Main Download Packages Table -->
      <section class="space-y-6">
        <DownloadTable />
      </section>

    </main>

    <!-- Clean Footer with Disclaimer and English language default -->
    <footer class="border-t border-zinc-900 bg-[#060608] py-8 text-xs text-zinc-500 font-mono mt-auto">
      <div class="max-w-5xl mx-auto px-6 flex flex-col sm:flex-row items-center justify-between gap-4">
        <!-- Unofficial Disclaimer -->
        <p class="text-[11px] text-zinc-500 max-w-xl">
          Disclaimer: This is an unofficial community site. Antigravity IDE and trademarks belong to Google LLC. Packages provided here are AppImage builds based on official Linux tar.gz releases distributed by Google.
        </p>

        <!-- Language Selector defaulted to English -->
        <div class="flex items-center space-x-2 cursor-pointer hover:text-zinc-300 transition-colors shrink-0">
          <span class="text-zinc-300 font-sans font-medium">English</span>
          <svg class="w-3.5 h-3.5 text-zinc-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
          </svg>
        </div>
      </div>
    </footer>

    <!-- Modals & Toasts -->
    <InstallModal />
    <Toast />
  </div>

  <!-- Interactive Client-side Scripting -->
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      // 1. Toast Notification Trigger
      const toast = document.getElementById('toast-notification');
      const toastMsg = document.getElementById('toast-message');

      function showToast(message: string) {
        if (!toast || !toastMsg) return;
        toastMsg.textContent = message;
        toast.classList.remove('translate-y-12', 'opacity-0');
        toast.classList.add('translate-y-0', 'opacity-100');

        setTimeout(() => {
          toast.classList.remove('translate-y-0', 'opacity-100');
          toast.classList.add('translate-y-12', 'opacity-0');
        }, 2500);
      }

      // 2. Clipboard Copy Handlers
      document.querySelectorAll<HTMLElement>('.copy-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
          e.preventDefault();
          const textToCopy = btn.getAttribute('data-text') || btn.getAttribute('data-hash');
          if (textToCopy) {
            navigator.clipboard.writeText(textToCopy);
            showToast('Copied to clipboard!');
          }
        });
      });

      // 3. Modal Controls for Package Downloads
      const modal = document.getElementById('install-modal');
      const modalClose = document.getElementById('modal-close');
      const modalDone = document.getElementById('modal-done-btn');
      const modalTitle = document.getElementById('modal-title');
      const modalCmds = document.getElementById('modal-commands');
      const directDownload = document.getElementById('direct-download-link') as HTMLAnchorElement;

      function openModal(format: string) {
        if (!modal || !modalTitle || !modalCmds || !directDownload) return;

        let titleText = 'Downloading Antigravity v1.4.0';
        let commandsHTML = '';
        let downloadFile = 'Antigravity-1.4.0-x86_64.AppImage';

        if (format === 'appimage') {
          titleText = 'Downloading Antigravity-1.4.0-x86_64.AppImage';
          downloadFile = 'Antigravity-1.4.0-x86_64.AppImage';
          commandsHTML = `
            <p class="text-zinc-500"># Make executable and run</p>
            <p>chmod +x Antigravity-1.4.0-x86_64.AppImage</p>
            <p>./Antigravity-1.4.0-x86_64.AppImage</p>
          `;
        } else if (format === 'deb') {
          titleText = 'Downloading antigravity_1.4.0_amd64.deb';
          downloadFile = 'antigravity_1.4.0_amd64.deb';
          commandsHTML = `
            <p class="text-zinc-500"># Install .deb package in Debian / Ubuntu</p>
            <p>sudo dpkg -i antigravity_1.4.0_amd64.deb</p>
          `;
        } else if (format === 'rpm') {
          titleText = 'Downloading antigravity-1.4.0.x86_64.rpm';
          downloadFile = 'antigravity-1.4.0.x86_64.rpm';
          commandsHTML = `
            <p class="text-zinc-500"># Install .rpm package in Fedora / RHEL</p>
            <p>sudo dnf install ./antigravity-1.4.0.x86_64.rpm</p>
          `;
        } else if (format === 'tar') {
          titleText = 'Downloading antigravity-1.4.0-linux-x64.tar.gz';
          downloadFile = 'antigravity-1.4.0-linux-x64.tar.gz';
          commandsHTML = `
            <p class="text-zinc-500"># Extract official Google binary tarball</p>
            <p>tar -xzf antigravity-1.4.0-linux-x64.tar.gz</p>
            <p>./antigravity/bin/antigravity</p>
          `;
        }

        modalTitle.textContent = titleText;
        modalCmds.innerHTML = commandsHTML;
        directDownload.setAttribute('href', `#download-${downloadFile}`);
        directDownload.setAttribute('download', downloadFile);

        modal.classList.remove('hidden');
        modal.classList.add('flex');
      }

      function closeModal() {
        if (!modal) return;
        modal.classList.remove('flex');
        modal.classList.add('hidden');
      }

      document.querySelectorAll('[data-modal-open]').forEach(trigger => {
        trigger.addEventListener('click', (e) => {
          e.preventDefault();
          const format = trigger.getAttribute('data-modal-open') || 'appimage';
          openModal(format);
        });
      });

      if (modalClose) modalClose.addEventListener('click', closeModal);
      if (modalDone) modalDone.addEventListener('click', closeModal);

      modal?.addEventListener('click', (e) => {
        if (e.target === modal) closeModal();
      });
    });
  </script>
</Layout>
